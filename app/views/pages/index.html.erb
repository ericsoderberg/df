<h1>Pages</h1>

<ul>
<% for page in @pages %>
  <li><%= link_to page.name,
    page_alias_path(:name => page.name.parameterize) %></li>
<% end %>
</ul>

<br />

<%= link_to 'New page', new_page_path, :class => 'admin' %>
