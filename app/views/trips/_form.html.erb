  <%= f.hidden_field :ministry_id %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name, :size => 40 %>
  </p>
  <table>
    <tr><td><%= f.label :start_at %></td>
      <td><%= f.label :stop_at %></td></tr>
    <tr><td>
      <%= f.calendar_date_select :start_at %></td>
      <td><%= f.calendar_date_select :stop_at %></td></tr>
  </table>
  <%= render :partial => '/data_files/list' %>
  <p>
    <%= f.label :description %><%= markdown_help %><br />
    <%= f.text_area :description, :size => "80x20" %>
  </p>
