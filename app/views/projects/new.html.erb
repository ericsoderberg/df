<h1>New project for <%= link_to @ministry.name, @ministry %></h1>

<% form_for(@project) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %> | <%= link_to 'Cancel', @ministry %>
  </p>
<% end %>
